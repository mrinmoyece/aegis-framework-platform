# API availability response

Confirm identity/governance readiness, separate client validation/denial from server
errors, inspect bounded API RED metrics and ledger freshness, and roll back only through
the normal release process. Never weaken authentication or authorization.
